# Project Agent Rules

## Git & GitHub Command Execution
- **Strict Permission Requirement**: Never execute `git` or `gh` (GitHub CLI) commands automatically.
- Always ask the user for explicit confirmation before running any git or github operations.
